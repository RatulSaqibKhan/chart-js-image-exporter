# Chart Js Image Builder

## Installation
- Install packages
  ```
  npm install
  ```
- Build using tsc
  ```
  npm run tsc
  ```
- Run in development mode
  ```
  npm run dev
  ```
- Run in production mode
  ```
  npm run prod
  ```